import React from "react";
import styles from "./logo.module.scss";

const Logo: React.FC = () => {
  return (
    <div className={styles.logo}>
      <h2>WWC</h2>
    </div>
  );
};

export default Logo;
